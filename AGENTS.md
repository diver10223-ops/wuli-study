# 项目开发规则

## 工作范围

- 每次只处理用户明确指定的一个知识模块或一个架构任务。
- 修改前必须检查并复用 `assets/` 下的共享引擎。
- 课程的逻辑阶段不等于独立物理文件；优先使用参数化页面和集中数据。
- 不得创建 `backup`、`copy`、`temp`、`old` 等备份文件。
- 未经用户确认，不得批量生成所有模块页面或重构整个课程体系。
- 如果预计新增超过 5 个源代码文件，必须先说明原因和替代方案。
- 不得仅以页面存在、数组数量或语法通过宣称课程内容完成。

## 忽略范围

- 不读取或修改 `node_modules/`、`dist/`、`build/`、`coverage/` 和缓存目录。
- 不把依赖、缓存、日志、临时文件或构建产物提交到 Git。
- 生成文件必须输出到约定的忽略目录，不得散落到仓库根目录。

## 内容完成标准

- 技术骨架完成、内容编写完成、教师审校完成和真实验证完成必须分别记录。
- 模板题不能计为正式题库完成。
- A/B/C 三轨必须有明确且可审查的内容差异。
- 正式卷必须能映射到知识矩阵和考卷规格。
- 结构审计不能替代物理正确性审校。

## 验证

- 优先执行当前模块的小范围检查，不在每次小改动后运行全量审计。
- JavaScript 配置应尽量在单个 Node.js 进程中批量验证。
- 全域回归只在提交前执行一次，并在 `执行结果.md` 记录实际结果。
- 完成后报告新增、修改、删除的文件数量。
